<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".main.RouteCreationActivity">

    <fragment
        android:layout_width="match_parent"
        android:layout_height="426dp"
        android:name="net.guardduty.main.GoogleMapFragment"
        android:id="@+id/route_creation_map"
        android:layout_gravity="center_horizontal|top" />

    <Button
        android:layout_width="160dp"
        android:layout_height="90dp"
        android:text="Add checkpoint"
        android:id="@+id/route_creation_add_checkpoint_button"
        android:layout_gravity="left|bottom"
        android:onClick="createCheckpoint" />

    <Button
        android:layout_width="160dp"
        android:layout_height="90dp"
        android:text="Finish route"
        android:id="@+id/route_creation_route_finish_button"
        android:layout_gravity="right|bottom"
        android:onClick="finishRoute" />
</FrameLayout>